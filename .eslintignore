# don't ever lint node_modules
node_modules
# don't lint build output
public
# don't lint cached files
.cache
# don't lint generated types
src/gatsby-types.d.ts
# don't lint lock and config files
# .husky
# .eslintignore
# package.json
# package-lock.json
